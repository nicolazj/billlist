import * as React from 'react';
import { ReactElement } from 'react';
import { Text, View } from 'react-native';

const BillList = (): ReactElement => {
  return (
    <View>
      <Text>TODO: Implement Bill List</Text>
    </View>
  );
};

export default BillList;
